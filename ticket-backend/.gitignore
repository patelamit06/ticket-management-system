node_modules
dist
.env
.env.local
.env.production
.env.*.local
*.log
coverage

# Stray TS build artifacts (prisma/ gets pulled into nest build's rootDir)
*.tsbuildinfo
prisma/seed.js
prisma/seed.js.map
prisma/seed.d.ts
.generated
# Prisma 7 generates client here; run `npm run prisma:generate` after clone
generated

# Swish mTLS certs / any local secrets — never commit
secrets/

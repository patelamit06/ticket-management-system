# Smart prompts – Sales dashboard & reports export

**Backend:** NestJS sales aggregates per event (tickets sold, revenue, by type, trends); CSV/PDF export endpoints. Resource guard for organizer. DTOs and Swagger.

**Frontend:** Sales dashboard with charts (recharts), tables, export buttons. See docs/features/12-sales-reports/ for task lists.
